"""Command line entry point for Codex AI Gateway."""

from __future__ import annotations


def main() -> None:
    """Start the gateway (placeholder, real uvicorn run is in app)."""
    raise SystemExit(
        "Use `uv run uvicorn codex_ai_gateway.app:app` to start the gateway."
    )
